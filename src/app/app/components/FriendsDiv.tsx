import styles from '../page.module.css';

import React, { useEffect, useState } from 'react';
import { Currents } from '../utils/utils';

interface Props {
    Currents: Currents,
}

const FriendsDiv: React.FC<Props> = ({ Currents }) => {
    return (
        <>
            
        </>
    );
}

export default FriendsDiv;